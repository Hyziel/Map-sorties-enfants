/** Valeurs partagées par plusieurs pages, à un seul endroit. */

/** Éditeur du site, tel qu'il figure dans les mentions légales et le pied de page. */
export const EDITEUR = 'Louise Barbarin';

export const ANNEE = new Date().getFullYear();

/** Adresse publique du site, utilisée par le sitemap et les données structurées. */
export const SITE = 'https://sorties-enfants-geneve.netlify.app';
